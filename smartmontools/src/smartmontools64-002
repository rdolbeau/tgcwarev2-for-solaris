--- smartmontools-6.4/regex/regex.h.orig	2012-12-13 17:23:06.000000000 +0000
+++ smartmontools-6.4/regex/regex.h	2015-10-26 15:31:42.885628000 +0000
@@ -536,11 +536,7 @@
 #endif
 /* gcc 3.1 and up support the [restrict] syntax.  */
 #ifndef __restrict_arr
-# if (__GNUC__ > 3 || (__GNUC__ == 3 && __GNUC_MINOR__ >= 1)) && !defined(__WIN32__)
-#  define __restrict_arr __restrict
-# else
 #  define __restrict_arr
-# endif
 #endif
 
 /* POSIX compatibility.  */
